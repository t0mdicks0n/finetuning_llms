# Modal authentication is handled via the CLI:
#   pip install modal
#   modal token new
# This stores credentials in ~/.modal.toml (not in .env)

# HuggingFace (optional - for pushing models)
# HF_TOKEN=your_huggingface_token_here
