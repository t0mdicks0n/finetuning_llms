[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
pymupdf = ">=1.24.0"
requests = ">=2.31.0"
transformers = ">=4.44.0"
datasets = ">=2.20.0"
accelerate = ">=0.33.0"
peft = ">=0.12.0"
trl = ">=0.9.0"
modal = ">=0.64.0"
pandas = ">=2.0.0"
numpy = ">=1.24.0"
scikit-learn = ">=1.3.0"
tqdm = ">=4.66.0"
python-dotenv = ">=1.0.0"
huggingface-hub = ">=0.24.0"
google-generativeai = "*"
ftfy = "*"

[dev-packages]
pytest = ">=8.0.0"
black = ">=24.0.0"
ruff = ">=0.5.0"

[requires]
python_version = "3.13"
